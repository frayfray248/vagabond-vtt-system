
const { SchemaField, NumberField } = foundry.data.fields;

export default class SystemActorDataModel extends foundry.abstract.TypeDataModel<{}, any> {
    static defineSchema() {
        return {
        }
    }
}